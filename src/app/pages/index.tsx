import React, { useEffect } from "react";
import PageLayout from "../layout/layout";

const Landing = () => {
  useEffect(() => {
  });
  return <PageLayout>
    
  </PageLayout>
};

export default Landing;
